package mx.att.digital.api.models.enums;

/**
 * The Enum State.
 */
public enum State {
    
    /** The created. */
    CREATED,
    
    /** The failed. */
    FAILED,
    
    /** The completed. */
    COMPLETED,
    
    /** The cancelled. */
    CANCELLED
}
